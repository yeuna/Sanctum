// IMPORTANT: the very first line of an autoconfig file is ALWAYS skipped by the
// parser, so this comment must stay here. Do not move the lockPref calls up.
// -----------------------------------------------------------------------------
//  Sanctum lock file.
//  These are the "holy" preferences: anything that could transmit who you are
//  or what hardware you run. lockPref() freezes them so that NO extension,
//  enterprise policy, profile import, sync payload, or accidental about:config
//  edit can ever turn data submission back on. They appear greyed-out and
//  unchangeable in about:config and about:preferences.
// -----------------------------------------------------------------------------

// ---- Master data-submission gate ----
lockPref("datareporting.policy.dataSubmissionEnabled", false);
lockPref("datareporting.healthreport.uploadEnabled", false);

// ---- Telemetry pipeline ----
lockPref("toolkit.telemetry.unified", false);
lockPref("toolkit.telemetry.enabled", false);
lockPref("toolkit.telemetry.server", "data:,");
lockPref("toolkit.telemetry.archive.enabled", false);
lockPref("toolkit.telemetry.newProfilePing.enabled", false);
lockPref("toolkit.telemetry.shutdownPingSender.enabled", false);
lockPref("toolkit.telemetry.coverage.opt-out", true);
lockPref("toolkit.coverage.opt-out", true);
lockPref("toolkit.coverage.endpoint.base", "");

// ---- Studies / remote control ----
lockPref("app.shield.optoutstudies.enabled", false);
lockPref("app.normandy.enabled", false);
lockPref("app.normandy.api_url", "");
lockPref("messaging-system.rsexperimentloader.enabled", false);

// ---- Crash reporting (binary also compiled out) ----
lockPref("breakpad.reportURL", "");
lockPref("browser.tabs.crashReporting.sendReport", false);

// ---- Activity-stream / ping-centre transport ----
lockPref("browser.ping-centre.telemetry", false);
lockPref("browser.newtabpage.activity-stream.telemetry", false);
lockPref("browser.newtabpage.activity-stream.feeds.telemetry", false);

// ---- Windows default-browser agent (binary also compiled out) ----
lockPref("default-browser-agent.enabled", false);

// ---- Hardware / fingerprint identity ----
lockPref("privacy.resistFingerprinting", true);
